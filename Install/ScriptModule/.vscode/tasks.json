{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            
            "command": "cd ../Debug && NFPluginLoader --Server=RebotServer --ID=20 --Path=../Config --LuaScript=../ScriptModule",
            //"command": "cd ../Debug && NFPluginLoader --Server=AllServer --ID=11 --Path=../Config --LuaScript=../ScriptModule",
            "problemMatcher": []
        }
    ]
}