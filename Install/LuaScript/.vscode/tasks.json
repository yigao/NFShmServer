{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "cd ../Bin/Dynamic_Debug && ./NFPluginLoader --Server=AllServer --ID=1.13.1.1 --Config=../../Config/MMO --Plugin=../../MMOBusPlugin --restart",
            "problemMatcher": []
        }
    ]
}